#in = usado no lugar do == as vezes com str
# == comparador se sao iguais normamente para numeros
# != quer dizer que o num1 != num2
 3 aspas num print('''algo
mais algo''') usado pra imprimir em linhas separadas
variavel n1 exmplo
if = a se ultiliza if n1 > 0:
else = senao, quer  dizer que n1 < 0:
elif = e se  continua o raciocinio do if
end= '' num print faz que ele imprima numa linha so

#tipo = str(input('Quer Par ou Impar[P/I]: ')).upper().strip()[0] USADO para repetir a pergunta se nao for a resposta
        #while tipo not in 'PI':                                  que se pede no caso PI se digitar algo diferente se repete ate a resp ser P ou I
                #tipo = str(input('Quer Par ou Impar[P/I]: ')).upper().strip()[0]

# em comparadores ideal usar apenas If nao usar elif
#     if resposta == 'N': quando for digitado como exemplo N para o laco
#         break        com o comando break
# while True  para laco sem fim  True com T maiusculo pra reconhcer se for minsculo no vai
not in  mesma coisa que diferente != usado em comparacao de palavras ou letras
print('\n') serve para pular uns 3 paragrafo